[core]
	engine = pg
	# plan_file = migrations/sqitch.plan
	top_dir = migrations
[engine "pg"]
	target = db:pg://spedata:spedata@localhost:5432/starrynight
    # OU target = db:pg:starrynight
	# registry = sqitch
	# client = psql